<UserControl  xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"  xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol" x:Class="MediTech.Views.ListVendors"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
              DataContext="{Binding Path=ListVendorViewModel,Source={StaticResource Locator}}"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <dxlc:LayoutControl Orientation="Vertical">
            <dxg:GridControl x:Name="grdUsers" ItemsSource="{Binding VendorDetails}"
                             SelectedItem="{Binding SelectVendorDetail}">
                <dxg:GridControl.Columns>
                    <dxg:GridColumn Header="ชื่อ" FieldName="CompanyName"/>
                    <dxg:GridColumn Header="ประเภท" FieldName="VendorType"/>
                    <dxg:GridColumn Header="เลขประจำตัวผู้เสียภาษี" FieldName="TINNo"/>
                    <dxg:GridColumn Header="ผู้ติดต่อ" FieldName="Description"/>
                    <dxg:GridColumn Header="เบอร์โทร" FieldName="ContactPerson"/>
                    <dxg:GridColumn Header="เบอร์ Fax" FieldName="FaxNo"/>
                    <dxg:GridColumn Header="อีเมลล์" FieldName="Email"/>
                    <dxg:GridColumn Header="ที่อยู่" FieldName="AddressFull"/>
                </dxg:GridControl.Columns>
                <dxg:GridControl.View>
                    <dxg:TableView ShowGroupPanel="False"  ShowSearchPanelMode="Always" 
                                   SearchPanelNullText="ค้นหา" AllowEditing="False" />
                </dxg:GridControl.View>
            </dxg:GridControl>
            <dxlc:LayoutGroup Orientation="Horizontal" HorizontalAlignment="Right" >
                <dx:SimpleButton Width="80" Command="{Binding AddCommand}"
                                     Height="25" Content="เพิ่ม"/>
                <dx:SimpleButton Width="80" Command="{Binding EditCommand}"
                                     Height="25" Content="แก้ไข"/>
                <dx:SimpleButton Width="80" Margin="50,0,0,0" Command="{Binding DeleteCommand}"
                                     Height="25" Content="ลบ"/>
            </dxlc:LayoutGroup>
        </dxlc:LayoutControl>
    </Grid>
</UserControl>
