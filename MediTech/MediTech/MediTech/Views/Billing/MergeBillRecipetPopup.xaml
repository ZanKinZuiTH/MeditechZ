<UserControl x:Class="MediTech.Views.Billing.MergeBillRecipetPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediTech.Views.Billing" xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             mc:Ignorable="d" 
             xmlns:cv="clr-namespace:MediTech.Converter"
             Height="180"
             Width="500"
             DataContext="{Binding Path=MergeBillRecipetPopupViewModel,Source={StaticResource Locator}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <cv:NumericN2Formatter x:Key="NumericN2Formatter" />
    </UserControl.Resources>
    <AdornerDecorator>
        <dxlc:LayoutControl Orientation="Vertical" ItemSpace="15">


            <dxlc:LayoutGroup Orientation="Horizontal" VerticalAlignment="Center">
                <dxlc:LayoutItem Label="Source Payor" >
                    <ComboBox ItemsSource="{Binding Path= PatientVisitPayors,UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="PayorName" SelectedItem="{Binding Path=SelectedSourceVisitPayor,UpdateSourceTrigger=PropertyChanged}"/>
                </dxlc:LayoutItem>
                <dxlc:LayoutItem HorizontalAlignment="Right" Margin="10,0,0,0">
                    <TextBlock Text="{Binding SourcePayorAmount, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={StaticResource NumericN2Formatter}}" FontWeight="Bold" Foreground="Blue"/>
                </dxlc:LayoutItem>
            </dxlc:LayoutGroup>

            <dxlc:LayoutGroup Orientation="Horizontal" VerticalAlignment="Center">
                <dxlc:LayoutItem Label="Destination Payor">
                    <ComboBox ItemsSource="{Binding Path= PatientVisitPayors,UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="PayorName" SelectedItem="{Binding Path=SelectedDestinationVisitPayor,UpdateSourceTrigger=PropertyChanged}"/>
                </dxlc:LayoutItem>
                <dxlc:LayoutItem HorizontalAlignment="Right" Margin="10,0,0,0">
                    <TextBlock Text="{Binding DestinationPayorAmount, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={StaticResource NumericN2Formatter}}" FontWeight="Bold" Foreground="Red"/>
                </dxlc:LayoutItem>
            </dxlc:LayoutGroup>
       
            <dxlc:LayoutGroup VerticalAlignment="Bottom" >
                <dxlc:LayoutItem HorizontalAlignment="Left">
                    <TextBlock Text="***Receipt Only" Foreground="Red"/>
                </dxlc:LayoutItem>
                <dxlc:LayoutItem  HorizontalAlignment="Right">
                    <DockPanel  HorizontalAlignment="Stretch">
                        <Button  DockPanel.Dock="Right" Name="CancelButton" VerticalAlignment="Center" Width="80" IsCancel="True" Content="ยกเลิก" Command="{Binding CancelCommand}" />
                        <Button DockPanel.Dock="Right"  Name="SaveButton" VerticalAlignment="Center" Width="80" IsDefault="True" Content="บันทึก" Command="{Binding SaveCommand}" Margin="0,0,20,0"/>
                    </DockPanel>
                </dxlc:LayoutItem>
            </dxlc:LayoutGroup>

        </dxlc:LayoutControl>
    </AdornerDecorator>
</UserControl>
