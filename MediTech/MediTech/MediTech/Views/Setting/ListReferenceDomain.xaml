<UserControl xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"  xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"  xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol" x:Class="MediTech.Views.ListReferenceDomain" 
             mc:Ignorable="d"
             DataContext="{Binding Path=ListReferenceDomainViewModel, Source={StaticResource Locator}}"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>

        <dxlc:LayoutControl Orientation="Vertical">
            <dxlc:LayoutGroup Header="LayoutGroup" Orientation="Horizontal">
                <dxlc:LayoutItem Label="Doamin Code">
                    <dxe:TextEdit Text="{Binding DomainCode}"/>
                </dxlc:LayoutItem>
                <dxlc:LayoutItem Label="Description">
                    <dxe:TextEdit Text="{Binding DomainDescription}"/>
                </dxlc:LayoutItem>
                <dxlc:LayoutItem Label="Value Code/Description">
                    <dxe:TextEdit Text="{Binding ValueDescription}"/>
                </dxlc:LayoutItem>
            </dxlc:LayoutGroup>
            <dx:SimpleButton
                HorizontalAlignment="Right"
                Command="{Binding SearchCommand}"
                Width="80"
                 Height="25" Content="ค้นหา"/>
            <dxg:GridControl x:Name="grdReferenceDomain" ItemsSource="{Binding ReferenceDomainSource}"
                             SelectedItem="{Binding SelectReferenceDomain}">
                <dxg:GridControl.Columns>
                    <dxg:GridColumn Header="DomainCode" FieldName="DomainCode" ReadOnly="True"/>
                    <dxg:GridColumn Header="Description" FieldName="Description" ReadOnly="True"/>
                </dxg:GridControl.Columns>
                <dxg:GridControl.View>
                    <dxg:TableView ShowGroupPanel="False" />
                </dxg:GridControl.View>
            </dxg:GridControl>
            <dxlc:LayoutGroup HorizontalAlignment="Right">
                <dx:SimpleButton Width="80" Command="{Binding AddCommand}"
                                     Height="25" Content="เพิ่ม"/>
                <dx:SimpleButton Width="80" Command="{Binding EditCommand}"
                                     Height="25" Content="แก้ไข"/>
                <dx:SimpleButton Width="80" Margin="50,0,0,0" Command="{Binding DeleteCommand}"
                                     Height="25" Content="ลบ"/>
            </dxlc:LayoutGroup>
        </dxlc:LayoutControl>

    </Grid>
</UserControl>
