<UserControl x:Class="MediTech.Views.AdjustOrderDetailForPackage" xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"  xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"  xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"  xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediTech.Views"
             mc:Ignorable="d"
             DataContext="{Binding Path=ManageOrderSetViewModel,Source={StaticResource Locator}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <dxlc:LayoutControl Orientation="Vertical">
            <dxlc:LayoutGroup Orientation="Horizontal" HorizontalAlignment="Left" >
                <dxlc:LayoutItem VerticalAlignment="Center" VerticalContentAlignment="Center">
                    <TextBlock Text="{Binding Packagename}" FontSize="16" FontWeight="Bold"/>
                </dxlc:LayoutItem>
                <dxlc:LayoutItem Label=" By :" VerticalContentAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="{Binding PackageCreateBy}" />
                </dxlc:LayoutItem>
            </dxlc:LayoutGroup>
            <dxg:GridControl x:Name="grdOrderDetail" ItemsSource="{Binding OrderDetails}"
                             SelectedItem="{Binding SelectPatientOrderDetail}"
                             >
                <dxg:GridColumn Header="" FieldName="IsSelected" Width="30">
                    <dxg:GridColumn.EditSettings>
                        <dxe:CheckEditSettings />
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>
                <dxg:GridColumn FieldName="Code" ReadOnly="True"/>
                <dxg:GridColumn Header="ItemName" FieldName="ItemName" ReadOnly="True"/>
                <dxg:GridColumn Header="StartDttm" FieldName="StartDttm" ReadOnly="True">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings DisplayFormat="dd/MM/yyyy HH:MM"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>
                <dxg:GridColumn Header="EndDttm" FieldName="EndDttm" ReadOnly="True">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings DisplayFormat="dd/MM/yyyy HH:MM"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>
                <dxg:GridColumn Header="Quantity" FieldName="Quantity" ReadOnly="True"/>
                <dxg:GridColumn Header="NetAmount" FieldName="NetAmount" ReadOnly="True">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings DisplayFormat="#,#.00"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>
                <dxg:GridColumn Header="OrderLocation" FieldName="OrderLocation" ReadOnly="True"/>
                <dxg:GridColumn Header="OrderToLocation" FieldName="OrderToLocation" ReadOnly="True"/>
                <dxg:GridColumn Header="OrderRaisedBy" FieldName="OrderRaisedBy" ReadOnly="True"/>
                <dxg:GridColumn Header="OrderCategory" FieldName="OrderCategory" ReadOnly="True"/>
                <dxg:GridColumn Header="OrderSubCategory" FieldName="OrderSubCategory" ReadOnly="True"/>
                <dxg:GridColumn Header="Status" FieldName="Status" ReadOnly="True"/>
                <dxg:GridColumn Header="PaymentStatus" FieldName="PaymentStatus" ReadOnly="True"/>
                <dxg:GridColumn Header="Note Processing" FieldName="Comment" ReadOnly="True"/>
                <dxg:GridControl.View>
                    <dxg:TableView ShowGroupPanel="False"/>
                </dxg:GridControl.View>
            </dxg:GridControl>
            <dxlc:LayoutGroup HorizontalAlignment="Right"
                              
                              Orientation="Horizontal">
                <dx:SimpleButton Width="80" Command="{Binding SaveCommand}"
                                     Height="25" Content="บันทึก"/>
                <dx:SimpleButton Width="80" Command="{Binding CancelCommand}"
                                     Height="25" Content="ยกเลิก"/>
            </dxlc:LayoutGroup>
        </dxlc:LayoutControl>

    </Grid>
</UserControl>
