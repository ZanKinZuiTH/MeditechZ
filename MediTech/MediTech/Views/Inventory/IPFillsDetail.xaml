<UserControl x:Class="MediTech.Views.IPFillsDetail"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"  
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"  
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediTech.Views"
             mc:Ignorable="d" 
             DataContext="{Binding Path=IPFillsDetailViewModel, Source={StaticResource Locator}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <dxlc:LayoutControl Orientation="Vertical">
            <dxlc:LayoutGroup>
                <dxlc:LayoutItem Label="Fill ID">
                    <dxlc:LayoutItem Label="{Binding FillID}" FontWeight="Bold"/>
                </dxlc:LayoutItem>
                
                <dxlc:LayoutItem Label="Fill Date">
                    <dxlc:LayoutItem Label="{Binding FillDate}" FontWeight="Bold"/>
                </dxlc:LayoutItem>
                
                <dxlc:LayoutItem Label="Filled By">
                    <dxlc:LayoutItem Label="{Binding FilledBy}" FontWeight="Bold"/>
                </dxlc:LayoutItem>
            </dxlc:LayoutGroup>
            <dxg:GridControl AutoExpandAllGroups="True" ItemsSource="{Binding FillDetail}" SelectedItem="{Binding SelectFillDetail}">
                <dxg:GridControl.Columns>
                    <dxg:GridColumn Header="Patient ID" FieldName="PatientID" ReadOnly="True" GroupIndex="1"/>
                    <dxg:GridColumn Header="Patient Name" FieldName="PatientName" ReadOnly="True"/>
                    <dxg:GridColumn Header="Ward" FieldName="WardName" ReadOnly="True" GroupIndex="0"/>
                    <dxg:GridColumn Header="Bed" FieldName="BedName" ReadOnly="True" Width="80"/>
                    <dxg:GridColumn Header="Order Name" FieldName="ItemName" ReadOnly="True"/>
                    <dxg:GridColumn Header="Quantity" FieldName="Quantity" ReadOnly="True"/>
                    <dxg:GridColumn Header="UOM" FieldName="IMUOM" ReadOnly="True"/>
                    <dxg:GridColumn Header="Batch ID" FieldName="BatchID" ReadOnly="True"/>
                    <dxg:GridColumn Header="Expiry Date" FieldName="ExpiryDttm" ReadOnly="True"/>
                </dxg:GridControl.Columns>
                <dxg:GridControl.View>
                    <dxg:TableView  ShowColumnHeaders="True" AllowEditing="False" AllowGrouping="True" ShowGroupPanel="False" />
                </dxg:GridControl.View>
            </dxg:GridControl>
            <!--<dxg:GridControl Name="grdItemIssue" ItemsSource="{Binding FillDetail}"
                                 SelectedItem="{Binding SelectFillDetail}">
                <dxg:GridControl.Columns>
                    <dxg:GridColumn Header="Patient ID" FieldName="PatientID" ReadOnly="True"/>
                    <dxg:GridColumn Header="Patient Name" FieldName="PatientName" ReadOnly="True"/>
                    <dxg:GridColumn Header="Ward" FieldName="WardName" ReadOnly="True"/>
                    <dxg:GridColumn Header="Bed" FieldName="BedName" ReadOnly="True"/>
                    <dxg:GridColumn Header="Order Name" FieldName="ItemName" ReadOnly="True"/>
                    <dxg:GridColumn Header="Quantity" FieldName="Quantity" ReadOnly="True"/>
                    <dxg:GridColumn Header="UOM" FieldName="IMUOM" ReadOnly="True"/>
                    <dxg:GridColumn Header="Batch ID" FieldName="BatchID" ReadOnly="True"/>
                    <dxg:GridColumn Header="Expiry Date" FieldName="ExpiryDttm" ReadOnly="True"/>
                </dxg:GridControl.Columns>
                <dxg:GridControl.View>
                    <dxg:TableView ShowGroupPanel="False"
                                   AllowEditing="False" />
                </dxg:GridControl.View>
            </dxg:GridControl>-->
            
            <dxlc:LayoutGroup Orientation="Horizontal" HorizontalAlignment="Right">
                <dx:SimpleButton Width="80" Command="{Binding PrintAllCommand}"
                                     Height="25" Content="Print Presc."/>
                <!--<dx:SimpleButton Width="80" Command="{Binding PrintStickerAllCommand}"
                                     Height="25" Content="Print Sticker All"/>
                <dx:SimpleButton Width="80" Command="{Binding PrintCommand}"
                                     Height="25" Content="Print"/>
                <dx:SimpleButton Width="80" Command="{Binding PrintStickerCommand}"
                                     Height="25" Content="Print Sticker"/>-->
                <dx:SimpleButton Width="80" Command="{Binding CloseCommand}"
                                     Height="25" Content="Close"/>
            </dxlc:LayoutGroup>
        </dxlc:LayoutControl>
    </Grid>
</UserControl>
