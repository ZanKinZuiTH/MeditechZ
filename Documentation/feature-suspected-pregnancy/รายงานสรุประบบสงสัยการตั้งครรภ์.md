# รายงานสรุปการอัพเดทระบบ: สงสัยการตั้งครรภ์ (Suspected Pregnancy)

## 📋 สารบัญ
1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [โครงสร้างฐานข้อมูล](#โครงสร้างฐานข้อมูล)
3. [ส่วนประกอบของระบบ](#ส่วนประกอบของระบบ)
4. [การทำงานร่วมกันของระบบ](#การทำงานร่วมกันของระบบ)
5. [UX/UI Design](#uxui-design)
6. [รายงาน (Reports)](#รายงาน-reports)
7. [Web API](#web-api)
8. [สรุปงานที่ทำ](#สรุปงานที่ทำ)

---

## ภาพรวมระบบ

ระบบสงสัยการตั้งครรภ์ (Suspected Pregnancy) เป็นฟีเจอร์ที่เพิ่มเข้ามาในระบบ MediTech เพื่อบันทึกและจัดการข้อมูลสถานะการตั้งครรภ์ของผู้ป่วย โดยแบ่งเป็น 2 สถานะ:
- **สงสัยตั้งครรภ์ (IsSuspectedPregnant)**: สถานะที่ยังไม่ยืนยัน
- **ตั้งครรภ์ (IsPregnant)**: สถานะที่ยืนยันแล้ว

---

## โครงสร้างฐานข้อมูล

### ตาราง: PatientVitalSign

#### คอลัมน์ที่เพิ่มเข้ามา:
```sql
IsPregnant BIT NULL          -- สถานะตั้งครรภ์ (ยืนยัน)
IsSuspectedPregnant BIT NULL -- สถานะสงสัยตั้งครรภ์ (ยังไม่ยืนยัน)
```

#### ตำแหน่งในฐานข้อมูล:
- **Entity Framework Model**: `MediTechDataModel.edmx`
- **Entity Class**: `PatientVitalSign.cs`
- **Model Class**: `PatientVitalSignModel.cs`

#### ไฟล์ที่เกี่ยวข้อง:
- `MediTechData\MediTech.DataBase\PatientVitalSign.cs`
- `MediTechData\MediTech.Model\PatientHistory\PatientVitalSignModel.cs`
- `MediTechData\MediTech.DataBase\MediTechDataModel.edmx`

---

## ส่วนประกอบของระบบ

### 1. **Model Layer**

#### 1.1 PatientVitalSignModel.cs
```29:30:MediTechData\MediTech.Model\PatientHistory\PatientVitalSignModel.cs
public bool? IsPregnant { get; set; }
public bool? IsSuspectedPregnant { get; set; }
```

**หน้าที่**: 
- เก็บข้อมูลสถานะการตั้งครรภ์
- ใช้ `bool?` (nullable) เพื่อรองรับกรณีที่ยังไม่ได้บันทึกข้อมูล

---

### 2. **ViewModel Layer**

#### 2.1 PatientVitalSignViewModel.cs

**Properties ที่เพิ่มเข้ามา**:
```194:208:MediTech\MediTech\ViewModels\Patient\PatientVitalSignViewModel.cs
private bool _IsPregnant;

public bool IsPregnant
{
    get { return _IsPregnant; }
    set { Set(ref _IsPregnant, value); }
}

private bool _IsSuspectedPregnant;

public bool IsSuspectedPregnant
{
    get { return _IsSuspectedPregnant; }
    set { Set(ref _IsSuspectedPregnant, value); }
}
```

**Properties สำหรับ Recent Tab**:
```309:323:MediTech\MediTech\ViewModels\Patient\PatientVitalSignViewModel.cs
private bool _IsPregnantRe;

public bool IsPregnantRe
{
    get { return _IsPregnantRe; }
    set { Set(ref _IsPregnantRe, value); }
}

private bool _IsSuspectedPregnantRe;

public bool IsSuspectedPregnantRe
{
    get { return _IsSuspectedPregnantRe; }
    set { Set(ref _IsSuspectedPregnantRe, value); }
}
```

**Methods ที่เกี่ยวข้อง**:
- `AssingModelToProperties()`: โหลดข้อมูลจาก Model มาแสดงใน UI
- `AssingPropertiesToModel()`: บันทึกข้อมูลจาก UI ลง Model

---

#### 2.2 EnterPhysicalExamViewModel.cs

**การใช้งาน**:
```229:229:MediTech\MediTech\ViewModels\Checkup\EnterPhysicalExamViewModel.cs
|| PatientVitalSign.IsPregnant == true || PatientVitalSign.IsSuspectedPregnant == true)
```

**หน้าที่**: ตรวจสอบว่ามีการบันทึกข้อมูลการตั้งครรภ์หรือไม่ก่อนบันทึก Vital Sign

---

#### 2.3 TranslateCheckupResultViewModel.cs

**Logic การแปลผล**:
```527:536:MediTech\MediTech\ViewModels\Checkup\TranslateCheckupResultViewModel.cs
string pregnancyComment = "";
if (vitalSign.IsSuspectedPregnant ?? false)
{
    pregnancyComment = "สงสัยตั้งครรภ์";
}
else if (vitalSign.IsPregnant ?? false)
{
    pregnancyComment = "ตั้งครรภ์";
}
ResultComponentModel commentComponent = new ResultComponentModel() { ResultItemUID = 116, GPRSTUID = 3177, ResultItemCode = "PAR67", ResultItemName = "Comment", ResultValue = pregnancyComment };
```

**หน้าที่**: แปลผลการตรวจสุขภาพโดยเพิ่ม Comment เกี่ยวกับสถานะการตั้งครรภ์

---

### 3. **View Layer (UI)**

#### 3.1 PatientVitalSign.xaml

**Tab: Vital Sign (Tab หลัก)**

```170:187:MediTech\MediTech\Views\Patient\PatientVitalSign.xaml
<dxlc:LayoutGroup>
    <TextBlock Name="lblSuspectedPregnant" Text="สงสัยตั้งครรภ์" FontSize="14" />
    <dxe:CheckEdit  Height="25" EditValue="{Binding IsSuspectedPregnant}"
                    AllowNullInput="True"
                    Width="100" VerticalAlignment="Top"/>
    <TextBlock />
    <TextBlock />
    <TextBlock />
</dxlc:LayoutGroup>
<dxlc:LayoutGroup>
    <TextBlock Name="lblPregnant" Text="ตั้งครรภ์" FontSize="14" />
    <dxe:CheckEdit  Height="25" EditValue="{Binding IsPregnant}"
                    AllowNullInput="True"
                    Width="100" VerticalAlignment="Top"/>
    <TextBlock />
    <TextBlock />
    <TextBlock />
</dxlc:LayoutGroup>
```

**Tab: Recent (Tab แก้ไขข้อมูลเก่า)**

```412:425:MediTech\MediTech\Views\Patient\PatientVitalSign.xaml
<dxlc:LayoutGroup>
    <TextBlock Name="lblSuspectedPregnantRe" Text="สงสัยตั้งครรภ์" FontSize="14" />
    <dxe:CheckEdit Width="100"   EditValue="{Binding IsSuspectedPregnantRe}"  />
    <TextBlock />
    <TextBlock />
    <TextBlock />
</dxlc:LayoutGroup>
<dxlc:LayoutGroup>
    <TextBlock Name="lblPregnantRe" Text="ตั้งครรภ์" FontSize="14" />
    <dxe:CheckEdit Width="100"   EditValue="{Binding IsPregnantRe}"  />
    <TextBlock />
    <TextBlock />
    <TextBlock />
</dxlc:LayoutGroup>
```

**UI Components**:
- ใช้ `dxe:CheckEdit` (DevExpress CheckBox) สำหรับการเลือกสถานะ
- `AllowNullInput="True"` เพื่อให้สามารถยกเลิกการเลือกได้
- แสดงข้อความภาษาไทย: "สงสัยตั้งครรภ์" และ "ตั้งครรภ์"

---

#### 3.2 EnterPhysicalExam.xaml

**การแสดงผลในหน้าตรวจร่างกาย**:
```92:100:MediTech\MediTech\Views\Checkup\EnterPhysicalExam.xaml
<dxlc:LayoutGroup Orientation="Horizontal">
    <dxlc:LayoutItem Label="สงสัยตั้งครรภ์" FontSize="17">
        <dxe:CheckEdit  Height="25" EditValue="{Binding PatientVitalSign.IsSuspectedPregnant,Mode=TwoWay}"
                        VerticalAlignment="Top"/>
    </dxlc:LayoutItem>
    <dxlc:LayoutItem Label="ตั้งครรภ์" FontSize="17">
        <dxe:CheckEdit  Height="25" EditValue="{Binding PatientVitalSign.IsPregnant,Mode=TwoWay}"
                        VerticalAlignment="Top"/>
    </dxlc:LayoutItem>
</dxlc:LayoutGroup>
```

**คุณสมบัติ**:
- แสดงในรูปแบบ Horizontal Layout
- FontSize="17" เพื่อความชัดเจน
- Binding แบบ TwoWay เพื่อให้สามารถแก้ไขได้

---

## การทำงานร่วมกันของระบบ

### Flow การทำงาน

```
┌─────────────────────────────────────────────────────────────┐
│                   1. ผู้ใช้บันทึกข้อมูล                        │
│              (PatientVitalSign.xaml)                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│        2. ViewModel จัดการข้อมูล                            │
│    (PatientVitalSignViewModel.cs)                           │
│    - IsPregnant / IsSuspectedPregnant                      │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│        3. บันทึกลงฐานข้อมูล                                  │
│    (PatientHistoryController.cs)                            │
│    - ManagePatientVitalSign()                               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│        4. ใช้ข้อมูลในการแปลผล                                 │
│    (TranslateCheckupResultViewModel.cs)                     │
│    - TranslateProcess()                                    │
│    - สร้าง Comment: "สงสัยตั้งครรภ์" หรือ "ตั้งครรภ์"        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│        5. แสดงผลในรายงาน                                     │
│    (Various Report Files)                                   │
│    - CheckupPage1.cs                                        │
│    - CheckupBookLarge1.cs                                  │
│    - MedReport1.cs                                          │
└─────────────────────────────────────────────────────────────┘
```

### การบันทึกข้อมูล

**ใน PatientVitalSignViewModel.cs**:
```525:526:MediTech\MediTech\ViewModels\Patient\PatientVitalSignViewModel.cs
model.IsPregnant = IsPregnant;
model.IsSuspectedPregnant = IsSuspectedPregnant;
```

**ใน EnterPhysicalExamViewModel.cs**:
```229:232:MediTech\MediTech\ViewModels\Checkup\EnterPhysicalExamViewModel.cs
|| PatientVitalSign.IsPregnant == true || PatientVitalSign.IsSuspectedPregnant == true)
{
    PatientVitalSign.RecordedDttm = DateTime.Now;
    DataService.PatientHistory.ManagePatientVitalSign(PatientVitalSign, AppUtil.Current.UserID);
}
```

---

## UX/UI Design

### 1. **PatientVitalSign Screen**

#### Layout Structure:
```
┌─────────────────────────────────────────┐
│         Patient Banner                  │
├─────────────────────────────────────────┤
│  Tab: Vital Sign | Recent               │
│  ┌───────────────────────────────────┐ │
│  │  Height, Weight, BMI, BSA         │ │
│  │  Temperature, Pulse, BP           │ │
│  │  ...                               │ │
│  │  [ ] สงสัยตั้งครรภ์                │ │
│  │  [ ] ตั้งครรภ์                     │ │
│  │  สีผิว, สภาพร่างกายจิตใจทั่วไป      │ │
│  └───────────────────────────────────┘ │
│  [บันทึก] [ยกเลิก]                      │
└─────────────────────────────────────────┘
```

#### Design Features:
- **CheckBox Control**: ใช้ DevExpress CheckEdit
- **Label Text**: ภาษาไทยชัดเจน
- **Font Size**: 14px (Vital Sign Tab), 17px (EnterPhysicalExam)
- **Layout**: LayoutGroup แบบ Vertical
- **Allow Null**: สามารถยกเลิกการเลือกได้

### 2. **EnterPhysicalExam Screen**

#### Layout Structure:
```
┌─────────────────────────────────────────┐
│         Patient Request Banner          │
├─────────────────────────────────────────┤
│  น้ำหนัก, ส่วนสูง, ความดัน              │
│  ชีพจร, เส้นรอบเอว, เส้นรอบสะโพก        │
│  [ ] สงสัยตั้งครรภ์  [ ] ตั้งครรภ์      │
│  ─────────────────────────────────      │
│  [Request Item Name]                    │
│  [Result Components Grid]                │
│  [Save] [Cancel]                         │
└─────────────────────────────────────────┘
```

#### Design Features:
- **Horizontal Layout**: แสดง 2 CheckBox แบบแนวนอน
- **Font Size**: 17px (ใหญ่กว่า Vital Sign Screen)
- **Two-Way Binding**: สามารถแก้ไขและบันทึกได้

### 3. **UI Components Specifications**

| Component | Type | Properties | Value |
|-----------|------|------------|-------|
| CheckBox | dxe:CheckEdit | Height | 25px |
| CheckBox | dxe:CheckEdit | Width | 100px (Vital Sign) / Auto (Physical Exam) |
| CheckBox | dxe:CheckEdit | AllowNullInput | True |
| Label | TextBlock | FontSize | 14px (Vital Sign) / 17px (Physical Exam) |
| Label | TextBlock | Text | "สงสัยตั้งครรภ์" / "ตั้งครรภ์" |

---

## รายงาน (Reports)

### 1. **CheckupPage1.cs** (CheckupBookReport)

**การแสดงผล**:
```332:341:MediTech\MediTech\Reports\Operating\Checkup\CheckupBookReport\CheckupPage1.cs
if (wellnessResult.Contains("สงสัยตั้งครรภ์") == true)
{
    lbBMI.Text = "";
    lbObesity.Text = "สงสัยตั้งครรภ์";
}
else if (wellnessResult.Contains("ตั้งครรภ์") == true)
{
    lbBMI.Text = "";
    lbObesity.Text = "ตั้งครรภ์";
}
```

**หน้าที่**: 
- ตรวจสอบข้อความใน wellnessResult
- แสดงผลแทนค่า BMI หากพบสถานะการตั้งครรภ์
- ใช้ Label `lbObesity` แสดงผล

### 2. **CheckupBookLarge1.cs** (CheckupBookLargeSize)

**การแสดงผล**: เหมือนกับ CheckupPage1.cs
```208:216:MediTech\MediTech\Reports\Operating\Checkup\CheckupBookLargeSize\CheckupBookLarge1.cs
if (wellnessResult.Contains("สงสัยตั้งครรภ์") == true)
{
    lbBMI.Text = "";
    lbObesity.Text = "สงสัยตั้งครรภ์";
}
else if (wellnessResult.Contains("ตั้งครรภ์") == true)
{
    lbBMI.Text = "";
    lbObesity.Text = "ตั้งครรภ์";
}
```

### 3. **CheckupPage1.cs** (CheckupBookA5)

**การแสดงผล**: เหมือนกับรายงานอื่นๆ
```379:387:MediTech\MediTech\Reports\Operating\Checkup\CheckupBookA5\CheckupPage1.cs
if (wellnessResult.Contains("สงสัยตั้งครรภ์") == true)
{
    lbBMI.Text = "";
    lbObesity.Text = "สงสัยตั้งครรภ์";
}
else if (wellnessResult.Contains("ตั้งครรภ์") == true)
{
    lbBMI.Text = "";
    lbObesity.Text = "ตั้งครรภ์";
}
```

### 4. **CheckupCSR.cs**

**การแสดงผล**: เหมือนกับรายงานอื่นๆ
```177:185:MediTech\MediTech\Reports\Operating\Checkup\CheckupCSR.cs
if (wellnessResult.Contains("สงสัยตั้งครรภ์") == true)
{
    lbBMI.Text = "";
    lbObesity.Text = "สงสัยตั้งครรภ์";
}
else if (wellnessResult.Contains("ตั้งครรภ์") == true)
{
    lbBMI.Text = "";
    lbObesity.Text = "ตั้งครรภ์";
}
```

### 5. **MedReport1.cs** (MedicalCheckUpReport)

**การแสดงผล**: เหมือนกับรายงานอื่นๆ
```178:186:MediTech\MediTech\Reports\Operating\Checkup\MedicalCheckUpReport\MedReport1.cs
if (wellnessResult.Contains("สงสัยตั้งครรภ์") == true)
{
    lbBMI.Text = "";
    lbObesity.Text = "สงสัยตั้งครรภ์";
}
else if (wellnessResult.Contains("ตั้งครรภ์") == true)
{
    lbBMI.Text = "";
    lbObesity.Text = "ตั้งครรภ์";
}
```

### 6. **รายงานอื่นๆ ที่เกี่ยวข้อง**

- **AppointmentMobile.Designer.cs**: แสดงข้อความเตือน "สำหรับสตรี กรณีสงสัยตั้งครรภ์ โปรดแจ้งเจ้าหน้าที่เพื่องดตรวจเอกซเรย์"
- **Alien.Designer.cs**: แสดงผลการตรวจตั้งครรภ์ในใบรับรองแพทย์ต่างด้าว
- **WorkingHeightCertificate1.Designer.cs**: คำถามเกี่ยวกับการตั้งครรภ์
- **ConfinedSpaceCertificate1.Designer.cs**: คำถามเกี่ยวกับการตั้งครรภ์

---

## Web API

### PatientHistoryController.cs

#### 1. **SearchPatientVitalSign**
```53:54:WebApi\MediTechWebApi\Controllers\PatientHistoryController.cs
IsPregnant = p.IsPregnant,
IsSuspectedPregnant = p.IsSuspectedPregnant,
```

#### 2. **GetPatientVitalSignByUID**
```89:90:WebApi\MediTechWebApi\Controllers\PatientHistoryController.cs
IsPregnant = p.IsPregnant,
IsSuspectedPregnant = p.IsSuspectedPregnant,
```

#### 3. **GetPatientVitalSignByVisitUID**
```123:124:WebApi\MediTechWebApi\Controllers\PatientHistoryController.cs
IsPregnant = p.IsPregnant,
IsSuspectedPregnant = p.IsSuspectedPregnant,
```

#### 4. **ManagePatientVitalSign** (บันทึก/อัพเดท)
```170:171:WebApi\MediTechWebApi\Controllers\PatientHistoryController.cs
vitalsign.IsPregnant = model.IsPregnant;
vitalsign.IsSuspectedPregnant = model.IsSuspectedPregnant;
```

**API Endpoints**:
- `GET /Api/PatientHistory/SearchPatientVitalSign`
- `GET /Api/PatientHistory/GetPatientVitalSignByUID`
- `GET /Api/PatientHistory/GetPatientVitalSignByVisitUID`
- `POST /Api/PatientHistory/ManagePatientVitalSign`
- `DELETE /Api/PatientHistory/DeletePatientVitalSign`

---

## สรุปงานที่ทำ

### ✅ งานที่เสร็จสมบูรณ์

#### 1. **Database Layer**
- ✅ เพิ่มคอลัมน์ `IsPregnant` และ `IsSuspectedPregnant` ในตาราง `PatientVitalSign`
- ✅ อัพเดท Entity Framework Model
- ✅ อัพเดท Model Classes

#### 2. **Business Logic Layer**
- ✅ เพิ่ม Properties ใน `PatientVitalSignViewModel`
- ✅ เพิ่ม Logic การบันทึกข้อมูล
- ✅ เพิ่ม Logic การโหลดข้อมูล
- ✅ เพิ่ม Logic การแปลผลใน `TranslateCheckupResultViewModel`
- ✅ เพิ่ม Logic การตรวจสอบใน `EnterPhysicalExamViewModel`

#### 3. **Presentation Layer (UI)**
- ✅ เพิ่ม UI Components ใน `PatientVitalSign.xaml` (2 Tabs)
- ✅ เพิ่ม UI Components ใน `EnterPhysicalExam.xaml`
- ✅ ใช้ DevExpress CheckEdit Controls
- ✅ Binding ข้อมูลแบบ Two-Way

#### 4. **Reports**
- ✅ อัพเดท `CheckupPage1.cs` (CheckupBookReport)
- ✅ อัพเดท `CheckupBookLarge1.cs`
- ✅ อัพเดท `CheckupPage1.cs` (CheckupBookA5)
- ✅ อัพเดท `CheckupCSR.cs`
- ✅ อัพเดท `MedReport1.cs`
- ✅ อัพเดทรายงานอื่นๆ ที่เกี่ยวข้อง

#### 5. **Web API**
- ✅ อัพเดท `SearchPatientVitalSign` endpoint
- ✅ อัพเดท `GetPatientVitalSignByUID` endpoint
- ✅ อัพเดท `GetPatientVitalSignByVisitUID` endpoint
- ✅ อัพเดท `ManagePatientVitalSign` endpoint

### 📊 สถิติการเปลี่ยนแปลง

| Layer | ไฟล์ที่แก้ไข | จำนวนไฟล์ |
|-------|-------------|----------|
| Database | 3 | 3 |
| Model | 1 | 1 |
| ViewModel | 3 | 3 |
| View | 2 | 2 |
| Reports | 5+ | 5+ |
| Web API | 1 | 1 |
| **รวม** | **15+** | **15+** |

### 🔄 Flow การทำงาน

1. **บันทึกข้อมูล**: ผู้ใช้บันทึกข้อมูลในหน้าจอ Vital Sign หรือ Physical Exam
2. **Validation**: ระบบตรวจสอบความถูกต้องของข้อมูล
3. **บันทึกลงฐานข้อมูล**: ข้อมูลถูกบันทึกลงตาราง PatientVitalSign
4. **แปลผล**: ระบบใช้ข้อมูลในการแปลผลการตรวจสุขภาพ
5. **แสดงผลในรายงาน**: ข้อมูลถูกแสดงในรายงานต่างๆ

### 🎯 จุดเด่นของระบบ

1. **ความยืดหยุ่น**: รองรับทั้งสถานะ "สงสัย" และ "ยืนยัน"
2. **การใช้งานง่าย**: ใช้ CheckBox ที่เข้าใจง่าย
3. **การแสดงผลครบถ้วน**: แสดงผลในรายงานหลายรูปแบบ
4. **API Support**: รองรับการเข้าถึงผ่าน Web API
5. **Data Integrity**: ใช้ Nullable Boolean เพื่อรองรับกรณีที่ยังไม่ได้บันทึก

### 📝 หมายเหตุ

- ข้อมูลการตั้งครรภ์จะถูกบันทึกร่วมกับ Vital Sign อื่นๆ
- การแปลผลจะเพิ่ม Comment ใน Result Component
- รายงานจะแสดงผลแทนค่า BMI หากพบสถานะการตั้งครรภ์
- ระบบรองรับการแก้ไขข้อมูลใน Tab "Recent"

---

## สรุป

ระบบสงสัยการตั้งครรภ์ได้ถูกเพิ่มเข้ามาในระบบ MediTech อย่างครบถ้วน ครอบคลุมตั้งแต่ Database, Business Logic, UI, Reports และ Web API โดยมีการออกแบบที่ยืดหยุ่นและใช้งานง่าย พร้อมรองรับการแสดงผลในรายงานหลายรูปแบบ

---

**วันที่สร้างรายงาน**: 2024  
**เวอร์ชัน**: 1.0  
**สถานะ**: ✅ เสร็จสมบูรณ์

